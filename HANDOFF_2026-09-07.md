# HANDOFF 2026-09-07 — テンプレート版を全14フォームに展開し、生成経路を切り替えた

前回: [HANDOFF_2026-09-06.md](HANDOFF_2026-09-06.md)（プロンプト二重管理の解消・統合ハイブリッドの試作）

## 最終 HEAD

```
（このファイルのコミット）
```

別PCで作業を始める前に必ず:

```bash
git pull
```

`npm run check`（スタイル114件・ゴールデン**242件**）と `next build` はグリーン。

---

## このセッションの結論を先に

院長判断: **「音声はそこまで使用しないので、全部統合版にしましょう」**

| | 旧（〜09-06） | 新 |
|---|---|---|
| カルテ本文 | AI が全文を書く | `lib/buildKarteTemplate.js` が組む（**全14フォーム**） |
| AI の役割 | 全部 | **音声があるときだけ**「受診理由の統合」「♯既往のマージ」 |
| 音声なしの初診 | AI 1回 | **AI ゼロ**（決定論・即時・無料） |
| 甲状腺6フォーム | AI 1回 | **常に AI ゼロ**（音声入力を持たないフォーム） |
| AI が落ちたとき | 生成失敗 | JS 素組みのカルテを必ず返す |
| テスト | 151 | **242**（カルテ本文スナップショット25件・取りこぼし検査19件を新設） |

---

## 🔴 明日の朝いちばんにやること

**外来で 2〜3 例、実際に生成されたカルテを見てください。** 見るのは 3 点:

1. **並びが変わっていないか**（特に ＃病名 と ♯既往のあいだの空行、【アレルギー歴】の直前）
2. **落ちている情報がないか**（申し送りの □ 行、【生活情報】【仕事】）
3. **音声を使った回**があれば、現病歴と紹介元が混ざっているか・♯既往が重複していないか

おかしければ **詳細画面の「🅰 旧AI版で作り直す」** を押せば、その患者だけ従来の
AI 全文生成に戻せます（デプロイ不要）。押した回は「旧AI版で生成しました」と出ます。

数週間問題が出なければ、そのボタンと `lib/buildKartePrompt.js`（1,097行）は消してよい。

---

## 1. 生成経路（新しい形）

```
generateKarteText(form_type, form_data, callAI)     ← lib/generateKarte.js
  ├ buildMergePrompt()  → 音声入力が無ければ null（= AI を呼ばない）
  ├ callAI()            → 音声があるときだけ。統合の2点だけを頼む（全文生成の約1/10）
  └ buildKarteTemplate(form_type, form_data, { merged })   ← 常にここが本文を組む
```

経路A（フォーム送信）も経路B（詳細画面の再生成）も同じ関数を通る。
**「初回生成と再生成でカルテが違う」は構造的に起きない。**

詳細画面の「テンプレート版（試作）」ブロックは、それが本番の出力になったので削除した。

## 2. ★空行の扱いを統一した（見た目が変わる箇所）

旧プロンプトの「出力フォーマット」の雛形には、**同じプロンプト内の【空行ルール5条件】と
矛盾する空行**が残っていた。例:

- 1型・高血圧: ♯他院管理 と【アレルギー歴】の間に空行がある雛形 ↔ ルールは「空行なし」
- SAS: ♯他院管理 と【SASの症状】の間に空行

AI がどちらを採るかは**毎回変わっていた**。テンプレート版では**常にルール側**を採る。
結果、フォーム間で並びが揃った。**ここが今回いちばん見た目が変わる箇所。**

## 3. 🔴 展開の過程で見つかった実バグ 1 件

問診票の体重減少ボタンは **「あり / なし / 不明」**（画面に「3ヶ月以内に3kg以上」と定義が書いてある）。
`"あり（3kg以上）"` という値は **DM差分問診（DmDiffEditor）にしか存在しない**。

2026-09-06 のテンプレート試作は後者だけを見ていたため、**DM基本・1型で
体重減少の警告【⚠️ 体重減少あり…】と申し送りが一度も出ない**状態だった。

> ★教訓: **列挙値は UI の選択肢そのものを見て書く。** 似た文字列を推測で書かない。
> 取りこぼし検査の allow-list には「『あり（3kg以上）』のときだけ分岐する列挙値」と
> **理由まで書いてあった**。理由が間違っていても検査は通る = **allow-list に書いた理由は仮説**。

## 4. フィクスチャが実データと違っていた（2回目）

09-06 に続いてまた出た。**テストデータが実データと違うと、テストは通るのに本番で落ちる。**

- 🔴 **SAS**: `sasCategory` / `cpapPriorRecordsConfirmed` / `sasSymptoms` を `disease` 配下に
  置いていた（実際は `reason` / `symptom` 配下）。**プロンプトの SAS区分がずっと「未選択」のまま**
  スナップショットに保存されていた
- 1型・妊娠糖尿病の家族歴に、フォームに無い `hl`
- 小児1型に、フォームで聴取しない 飲酒・喫煙・健診・ワクチン・仕事・20歳時体重
- 高血圧の `thyroidAdded` / 1型の `echoNeck`・`echoAbdomen` / `weightLossAmount` は存在しないフィールド

⇒ **新しいフォームを足すときはフィクスチャを `initialData` から起こすこと。**

## 5. 旧プロンプトの雛形どおりにしなかった箇所（★院長に見てほしい）

1. **小児1型の `・GAD抗体` 以下の内訳を ＃1型糖尿病 の直下に移した**
   旧雛形は ♯他院管理 の後ろに置いていて、診断名と内訳の間に別疾患が挟まる並びだった。成人1型と揃えた
2. **DM差分問診の「過去のHbA1c指摘」「治療の希望」「自由記載」を申し送りに出すようにした**
   旧方式では AI に渡してはいたが、使うかどうかは毎回変わっていた（＝聴取した内容が消えることがあった）
3. **DM差分問診の重要既往が複数選択のとき**、1つしかない詳細欄をどれかに結び付けず
   `□重要既往の詳細：…` として申し送りに出す（1つだけ選択なら従来どおり ♯行に入れる）
4. **SAS の受診理由サマリーを JS で組むようにした**（旧方式では SAS区分すら文章に入らないことがあった）
5. **DM差分問診の家族歴（DM / HL）を【FH】に反映するようにした**（旧方式は指示だけで反映は AI 任せ）

## 6. 取りこぼし検査を全14フォームに広げた（`scripts/golden/coverage.test.mjs`）

form_data の全フィールドを 1 つずつ書き換えて、**カルテ本文か統合プロンプトのどちらかが変わるか**を見る。
どちらも変わらなければ、その項目はカルテに一切影響していない = **情報が黙って落ちている**。

- **音声入力なしの状態で検査する**（音声があると統合プロンプトが何でも運んでしまい、検査が緩くなる）
- 例外は理由付きで allow-list に登録。理由が書けないものは直す
- 「理由リストに載っているのに実は使われている」も検出する（棚卸し）
- 🔴 **新しいフォームを足すときは必ずここにも足すこと**

## 7. ⚠️ 院長判断待ち（今回は変えなかった）

- **甲状腺フォームは「20歳時の体重」「最大体重」「最大体重の年齢」を聴取しているのに、
  カルテに出していない。** 旧プロンプトから同じ。技師さんが入力した分が使われていない。
  → 出すなら 身長行に足します（他フォームと同じ形）。allow-list に ⚠️ 付きで記録済み
- `history.eyeVisiting` はフォームの UI にも無い遺物（`initialData` に残っているだけ）

## 8. コスト

音声なしの回は **AI 呼び出しがゼロ**になった（甲状腺は全例ゼロ）。
音声ありの回も、生成の入力が 6,363字 → 651字（90%減）。
音声整形（入力時の2回）は従来どおり残るので、総額では約 1/3 になる見込み。

## 9. 残課題（前回から継続）

- レート制限（`/api/generate*`）
- 操作ログ（audit_logs 相当）
- prompt caching は**入れない**（損益分岐が月392件・当院の頻度では書込代の方が高い）
- T1D / PedT1D / HTHL / GDM / RH の step 大規模再編（B案）
- 数週間後: `?legacy=1` と `lib/buildKartePrompt.js`（1,097行）の削除
