{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "MEMDIR=~/.claude/projects/C--Users-smats-Documents-projects-karte-voice-pad/memory; echo '=== [session開始] repo + memory 自動 pull ==='; git -C ~/Documents/projects/dm-clinic-karte pull --autostash origin main 2>&1 | tail -3 || true; git -C $MEMDIR pull --autostash origin main 2>&1 | tail -3 || true; echo ''; echo '=== MEMORY.md 索引 ==='; head -80 $MEMDIR/MEMORY.md 2>/dev/null || echo '(MEMORY.md 未取得)'; echo ''; echo '=== 問診アプリの最新 HANDOFF (全文) ==='; L=$(ls -t $MEMDIR/HANDOFF_TO_NEXT_*monshin*.md 2>/dev/null | head -1); if [ -n \"$L\" ]; then echo \"📋 $(basename $L)\"; cat \"$L\"; else echo '(monshin の HANDOFF 無し)'; fi; echo ''; echo '=== repo 内の最新 HANDOFF (技術・冒頭のみ) ==='; R=$(ls -t ~/Documents/projects/dm-clinic-karte/HANDOFF_*.md 2>/dev/null | head -1); if [ -n \"$R\" ]; then echo \"📋 $(basename $R)\"; sed -n '1,40p' \"$R\"; fi; echo ''; echo '=== git 状態 ==='; echo '--- dm-clinic-karte ---'; git -C ~/Documents/projects/dm-clinic-karte status --short; git -C ~/Documents/projects/dm-clinic-karte log --oneline -5; echo '--- memory ---'; git -C $MEMDIR status --short; git -C $MEMDIR log --oneline -3; echo ''; echo '⚠️ HANDOFF は $MEMDIR/HANDOFF_TO_NEXT_<日付>_monshin.md で作り、書いたら即コミットする (2026-09-06: 連番にして別セッションに上書きされ消失)'; echo '⚠️ コミット前に必ず npm run check (スタイル + ゴールデン) と next build'; echo ''; echo '=== session-start 自動取得完了 ==='"
          }
        ]
      }
    ]
  }
}
