# HANDOFF 2026-08-22（クリニック → 自宅）

## このセッションでやったこと（全て main に push 済み）

自宅では `git pull` すれば最新になります。Vercel も自動デプロイ済み。

| commit | 内容 |
|---|---|
| `921eb87` | 【その他】に **内分泌フォーム** 新設（HTHL ベース、病名選択欄なし） |
| `1e7746c` | 内分泌の家族歴に自由記入欄 |
| `4730113` | 頚部エコーに「希望なし」／家族歴自由記入を 誰が・病気名 の2欄に |
| `1f9e542` | その他の病名が出力されない不具合 + 療養計画書の条件化 + 家族歴の複数組対応 |
| `126de64` | **その他の病名「通院先＝その他」で病院名が落ちる不具合を全8フォームで修正** |
| `93ffa35` | 頚部・腹部エコー**両方**に「希望なし」追加 + `lib/echo.js` に集約 |
| `df5d102` | **バグ防止機構**（プロンプト同期チェッカー + ゴールデンテスト + CI） |

### 新設した内分泌フォーム（`/endocrine`）
HTHL のコピー。主病名（＃）の選択欄を持たず、医師が診察時に問診して記載する運用。
申し送りに `□主病名：医師の診察時に確定・記載` を全例付与。
生活習慣病（糖尿病/高血圧/脂質異常症）にチェックがある時だけ `□初回療養計画書を作成済` が出る。

### 直した不具合
1. **通院先「その他」→ 自由入力の病院名が `（その他）` に化けていた**（全8フォーム）。
   2026-05 の SAS 追加時から3ヶ月間ずっと。`lib/otherDiseases.js` に集約して解消。
2. **エコー「希望なし」が「当院で施行予定」に化ける**（未知の選択肢を全部潰す実装だった）。
   `lib/echo.js` に集約して解消。

### 導入したバグ防止機構
```bash
npm run check          # 同期チェック + ゴールデンテスト（コミット前に必ず）
npm test               # ゴールデンテスト 60件
npm run check:prompt-sync
npm run test:update    # スナップショット更新（差分を目視してからコミット）
```
GitHub Actions が push/PR で上記 + `next build` を実行。main 失敗時は issue 自動作成 → メール通知。
詳細は CLAUDE.md の「バグ防止機構」節。

---

## 🏠 自宅でやる作業：プロンプト差分 137行 のレビュー

同期チェッカーを入れた時点で、**経路A（初回生成）と経路B（再生成）のプロンプトに
既に 8フォーム計137行の差分があった**ことが判明しました。
現状は `scripts/prompt-sync-allow.json` に「既知の差分」として記録し、CI は緑にしてあります。

**揃えるとカルテ出力が変わる**ので、1件ずつ判断が必要です。以下、優先度順の私見。

### 🔴 実害がありそう（再生成でカルテの中身が変わる）

| フォーム | 差分 | 影響 |
|---|---|---|
| 小児1型 | A `④開示状況（クラスメート）` + `④開示状況（先生）` の2行 / B `④周囲への開示` の1行 | **再生成すると2軸が1軸に潰れる**。2026-04 に2軸化した改修が経路B に反映されていない |
| DM基本・1型 | A に `【糖尿病の症状】` セクションあり / B に無し | **再生成すると症状セクションが消える**。2026-04 追加分が B 未反映 |
| SAS | A のフッターに `目標HbA1c…` `基本採血なし` / B に無し | 再生成でフッターの行が変わる |
| 妊娠糖尿病 | B にだけ `妊娠前:○kg` が身長体重行にある | 再生成で項目が増える。どちらが正か要判断 |
| DM基本 | B にだけ `◎甲状腺3項目追加済（HL+甲状腺追加済の場合のみ）` | DM に不要では？ HTHL からのコピペ残りの可能性 |
| HTHL | A にだけ `【ルール追加】`（要望は【】付きで記載 等）3行 | B の出力で【診察にあたっての要望】の書式が崩れる可能性 |

### 🟡 たぶん出力は同じ（AI 向け注記の有無だけ）

- `【アレルギー歴】` vs `【アレルギー歴】（アレルギーなしなら「なし」…）`
- `【飲酒歴】` vs `【飲酒歴】（整形済みテキスト）`
- `【FH】…` vs `【FH】…（FH DMの場合は誰かも記載）`
- 空行ルールの文言（A は `（＃糖尿病/＃HT/＃HL等）` と例示あり、B は例示なし）

いずれも B（経路B）の方が注記が丁寧。**B に寄せて A を書き足す**のが素直だと思います。

### ⚪ 差分ではない（チェッカーの仕様）

- 全フォームの B に出る `（医師希望指定ありの場合）□⟨expr⟩`
  → B は `STAFF_FLAGS` 定数、A はインライン IIFE。**出力は同じ**。無視して OK。
- `⟨expr⟩⟨expr⟩⟨expr⟩…` の行
  → 音声入力ブロックの埋め込み数の違い。中身は同じ。無視して OK。

### 作業手順（自宅）

```bash
git pull
node scripts/check-prompt-sync.mjs        # 現状は全部 ✅（既知の差分として記録済み）
```

全差分を読みたい場合は `scripts/prompt-sync-allow.json` を直接見るか、
`prompt-sync-allow.json` を一時的に空 `{}` にして `node scripts/check-prompt-sync.mjs` を回すと
137行が全部出ます。

1件直すたびに：
```bash
npm run check                              # 同期チェック + ゴールデンテスト
node scripts/check-prompt-sync.mjs --update  # 差分が減ったら記録を更新
```

**注意**: 経路A/B を揃えるとカルテ出力が変わるので、
1フォーム直すごとに `/detail/[id]` で実際に再生成して出力を目視確認してください。
ゴールデンテストのスナップショットも変わるので `npm run test:update` → `git diff` で確認。

---

## 未着手・積み残し

- **甲状腺6フォームは同期チェッカー未対応**（1コンポーネント + `formType` prop 分岐で 1:1 対応にならない）
- レート制限（`/api/generate*`）— CLAUDE.md のセキュリティ監査から未着手
- 操作ログ（audit_logs 相当）— 同上
- 本番カナリア（デプロイ後の疎通確認）— 勤怠にはあるがこちらは未導入
- SessionStart / Stop フック — 勤怠にはあるがこちらは未導入（下記参照）

---

## ⚠️ クリニック↔自宅の運用について（2026-08-22 時点の実態）

| | 勤怠（karteplus-monorepo） | dm-clinic-karte |
|---|---|---|
| コード同期 | git | ✅ git（同じ） |
| 引き継ぎ | memory repo の `HANDOFF_TO_NEXT_*.md` | ✅ **リポジトリ root の `HANDOFF_*.md`**（このファイル） |
| Claude のメモリ | 別 git repo で同期 | ❌ **このマシンのローカルのみ。自宅とは共有されない** |
| SessionStart フック（自動 pull + memory/HANDOFF 読込） | ✅ | ❌ なし |
| Stop フック（未 push 警告） | ✅ | ❌ なし |

つまり **コードと HANDOFF は同期できるが、Claude のメモリは同期されない** 状態です。
自宅の Claude Code は、このリポジトリの CLAUDE.md とこの HANDOFF を読めば作業を継続できます。

memory も同期したい場合は、勤怠と同じ「memory 用の別 git repo + SessionStart フック」を
このプロジェクトにも作る必要があります（未着手、やるなら次回）。
