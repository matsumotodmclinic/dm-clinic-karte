# 📨 引き継ぎメモ (2026-04-27)

新セッション(別チャット)へ。**5/6 本番運用開始まで残りわずか**。

---

## 1. 新セッションで最初にやること

両プロジェクトを最新化:

```bash
cd C:\Users\smats\Documents\projects\kinkan-app
git pull origin main

cd ../dm-clinic-karte
git pull origin main
```

### 最新コミット確認

- **kinkan-app**: `d085f25 docs: 2026-04-26 セッションログを追加`
- **dm-clinic-karte**: `063128d docs: CLAUDE.md を大規模更新(2026-04-26 セッション知見を統合)`

---

## 2. 両プロジェクトの現状

### kinkan-app(勤怠管理)

- **本番稼働中、5/6 から実戦投入予定**
- Phase 2 セキュリティ実装完了(iron-session、write-proxy /api/db、RLS strict lockdown)
- 自己監査 Critical 5件 + High 6件 すべて修正済み(2026-04-25)
- ログイン失敗ロック・パスワード表示トグル・Excel 基本給列分離・監査ログ共通化 等

### dm-clinic-karte(問診カルテ)

- **音声入力 + AI 要約機能 完成**(2026-04-26〜27 実装)
  - `lib/speechRecognition.js`, `lib/voiceSummary.js`, `lib/voiceVocabulary.js`
  - 全6フォームに「病歴・経緯の聴取」step
  - 薬剤256件 + 上尾市医師会89件の語彙誤認識補正
- 一括完了化、空行ルール完全明文化、和暦自動計算 等

---

## 3. 残タスク(優先度順)

### 🔴 5/5 夜(運用前夜、最重要)

**本番DB掃除**(30分):
- `overtime_requests` の `note='ランダムテストデータ'` 13件を DELETE
- `time_records` で川目さんの 2026-04 `clock_out` を実態に戻す(11日分)
- `supabase/local/insert_doctor_april_2026.sql` 由来の 55 件の精査
- 詳細は kinkan-app の CLAUDE.md「TODO」セクション参照

### 🟡 まつ作業(運用開始前)

- マニュアル用スクショ撮影(31枚、最優先7枚はゲート/ログイン/PW変更/カレンダー/スタッフ設定/履歴タブ/Vercel環境変数)
- 詳細は `HANDOFF_TO_CLINIC_2026-04-25.md` の §5

### 🔵 商用化フェーズ(後日)

- Next.js 16 への大型アップグレード(残2脆弱性、破壊的変更多い)
- マルチテナント対応
- セキュリティ診断(プロ依頼、20万円前後)
- 法務レビュー(プライバシーポリシー・医療情報法)
- Mediseo 経由の他クリニック営業準備

### ⚪ 軽微・任意

- パート用 Excel フォーマット別建て(現状暫定対応で困ってない)
- 有給残日数管理(妻アナログでOK判定)
- 医師打刻エッジケース

---

## 4. 環境状態(変更なし)

### Vercel 環境変数(両プロジェクト設定済み)

#### kinkan-app
- ✅ `SECRET_COOKIE_PASSWORD`(32文字以上)
- ✅ `APP_GATE_PASSWORD`(共通PWゲート、senbei-kudamono-CGM)
- ✅ `NEXT_PUBLIC_SUPABASE_URL`、`_ANON_KEY`、`SUPABASE_SERVICE_KEY`

#### dm-clinic-karte
- ✅ `SECRET_COOKIE_PASSWORD`(kinkan とは別値)
- ✅ `APP_GATE_PASSWORD`
- ✅ `KINKAN_VERIFY_URL`、`KINKAN_LIST_URL`、`KINKAN_AUDIT_URL`(デフォルト値あり)
- ✅ `ANTHROPIC_API_KEY`、`SUPABASE_URL`、`SUPABASE_SERVICE_KEY`

### DB 状態(全 migration 適用済み)

#### kinkan-app(2026-04 適用分)
- 20260424 系: staff_salary_history、part_wage 移行、role 再設計、wednesday_long
- 20260425 系: login_lockout、phase2 RLS strict lockdown、audit_logs.app

#### dm-clinic-karte
- 全テーブル RLS 適用済(audit_logs, settings 等)

### 検証済み事実

- 全 staff bcrypt 移行済(plaintext_count = 0)
- leave_requests は全件 '承認' 統一
- 監査ログ共通化:dm-clinic-karte の操作 → kinkan-app /admin/logs に表示
- ログイン失敗5回ロック動作確認済
- Phase E RLS:全 18 テーブル SELECT 専用化

---

## 5. 設計書・ドキュメント

### kinkan-app
- `CLAUDE.md`(全仕様、最新)
- `docs/design/security-phase2-plan.md`(Phase G 含む全実装計画)
- `docs/design/role-redesign-plan.md`(完了済み、参考)
- `docs/manuals/`(改4納品済、4冊)

### dm-clinic-karte
- `CLAUDE.md`(2026-04-26 大規模更新済み、最新)
- `docs/design/voice-input-summary-plan.md`(音声入力、実装完了)

---

## 6. 新セッションで Claude Code に投げるプロンプト例

### A. 5/5 DB掃除を実施する場合

```
kinkan-app/CLAUDE.md の TODO セクションにある「5月仮運用開始前の本番DB掃除」を実施したい。

事前に以下を SELECT で確認してから、まつの承認を得て DELETE/UPDATE する:
1. overtime_requests で note='ランダムテストデータ' の13件
2. time_records の川目さん 2026-04 clock_out 11日分
3. supabase/local/insert_doctor_april_2026.sql 由来の 55 件

破壊的操作なので必ず承認を取ってから実施。
```

### B. 動作確認を体系的にやりたい場合

```
本日の最新状態(kinkan-app: d085f25, dm-clinic-karte: 063128d)で、
本番運用前の総合動作確認チェックリストを作って欲しい。

特に:
- 音声入力 + AI 要約(全6フォーム)
- ログイン失敗ロック
- パスワード表示トグル
- Excel 基本給/月給分離
- 監査ログのアプリ別フィルタ・バッジ

私が触る順序を、ロール別(管理者/事務長/リーダー/スタッフ/医師)で整理して。
```

### C. その他のタスクを進める場合

```
kinkan-app と dm-clinic-karte の HANDOFF_2026-04-27.md を読んで、
現状を把握してください。
今日は [○○] に取り組みたい。
```

---

## 7. もしトラブったら

### git pull で競合
```bash
git status
git stash
git pull
git stash pop  # 必要なら
```

### Vercel デプロイ失敗
- ビルドログ確認、SECRET_COOKIE_PASSWORD が原因なら 32文字以上に再設定

### ログイン失敗ロック解除
```sql
UPDATE staff SET failed_attempts = 0, locked_until = NULL
WHERE name = '対象スタッフ名';
```

### 音声入力(dm-clinic-karte)が動かない
- iPad Safari でマイク許可ダイアログを確認
- iOS 設定 → Safari → カメラ・マイクへのアクセス → 許可

---

## 8. 過去の引き継ぎメモ(参考)

- `HANDOFF_TO_CLINIC_2026-04-25.md`(自宅 → クリニック、朝)
- `HANDOFF_TO_HOME_2026-04-25.md`(クリニック → 自宅、夕方)

---

## 9. 一言

5/6 運用開始まで実質あと9日。やるべきは絞られています:
- **DB掃除**(5/5夜、必須)
- **スクショ撮影**(任意、マニュアル用)
- **動作確認**(任意、運用前の安心材料)

もう機能追加・改修は急がなくて OK のはず。落ち着いた仕上げ期間です 🎉
