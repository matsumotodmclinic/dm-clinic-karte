// UI セマンティック色トークン
//
// 勤怠アプリ (karteplus-monorepo: apps/kinkan/src/lib/uiTokens.ts) からの移植。
// 値は勤怠と完全に同一にしてある。片方だけ変えないこと。
//
// 院長の配色方針 (2026-07-06 確立):
//   - 色は「意味」を担う。強度は 1 色の濃淡でなく意味ごとの色系統で分ける
//   - 青=通常・構造 / 琥珀=強調・要注目 / 紺=確定・固定 / 緑=正常・完了 / 赤=危険 / グレー=中立
//   - アイコンは絵文字でなくテキスト記号・線画SVG + CSS color
//     (絵文字は OS 描画でカラー固定・立体的なので業務アプリでは幼く見える)
//
// 採用方針: **新規コードはこのトークンを参照する**。既存ページの直書き hex は
//   触ったついでに移行する (一斉置換はしない)。
//   「色味を変えたい」が将来このファイル 1 箇所の修正で済むようにするのが目的。

export const UI = {
  /** 青 = 通常・構造・基本機能 (アプリのベースカラー) */
  primary: { fg: '#1565c0', bg: '#e3f2fd' },
  /** 琥珀 = 強調・要注目・日常の要対応 */
  accent: { fg: '#f57f17', bg: '#fff8e1' },
  /** 紺 = 確定・固定・ロック */
  fixed: { fg: '#283593', bg: '#e8eaf6' },
  /** 緑 = 正常・完了・問題なし */
  success: { fg: '#2e7d32', bg: '#e8f5e9' },
  /** 橙 = 警告 (注意は要るが危険ではない) */
  warning: { fg: '#e65100', bg: '#fff3e0' },
  /** 赤 = 危険・エラー・不足 */
  danger: { fg: '#c62828', bg: '#ffebee' },
  /** グレー = 中立・設定・補助 */
  neutral: { fg: '#546e7a', bg: '#eceff1' },

  /** テキスト階調 */
  text: '#333',
  textMuted: '#666',
  textFaint: '#888',
  textDisabled: '#bbb',

  /** 面 */
  surface: '#fff',
  surfaceAlt: '#f5f7fa',
  border: '#eee',
}
