# HANDOFF 2026-08-28

## このセッションでやったこと（全て main に push 済み）

`git pull` すれば最新になります。Vercel も自動デプロイ済み。

| commit | 内容 |
|---|---|
| `1160845` | 経路A/B のプロンプト差分のうち**実害ある6件**を解消（137→111行） |
| `952fd74` | 発火しない「◎甲状腺3項目追加済」を削除し、申し送りの□行に一本化 |
| `17a2139` | 採血後の**糖尿病診断グレーゾーン**の確認結果を申し送りに追記できるようにした |
| `76b2538` | **DM差分問診**を 高血圧・脂質異常症 と 反応性低血糖 にも展開 |
| `f2e4186` | 同じく**内分泌**にも展開 |
| `adb7a23` | ゴールデンテストの日付伏せ処理が `CPR0.5` を巻き込んでいたのを修正 |

`npm run check` は **88件 pass**（60 → 88 に増えた）、`next build` グリーン。

---

## 1. プロンプト差分 137行 のレビュー（HANDOFF_2026-08-22 の宿題）

1件ずつコードまで当たって判定しました。**HANDOFF_2026-08-22 の🔴判定は1件間違っていました**。

### 直した（実害あり）

| # | 何 | 実害 |
|---|---|---|
| 1 | **GDM「妊娠前:○kg」が経路A に無い** | 唯一**メイン経路**（毎回通る道）の欠落。フォームで聞いているのにカルテに出ない。妊娠前BMIで管理方針が変わるので影響が大きい |
| 2 | **【糖尿病の症状】が経路B に完全欠落**（DM基本/1型/小児1型） | 指示行だけでなく **`dmSymptoms` のデータ自体をプロンプトに渡していなかった**。再生成すると症状セクションが必ず消える。2026-04 追加分が4ヶ月 B 未反映 |
| 3 | 小児1型の開示状況が B では1軸 | 再生成でクラスメート/先生の2軸が潰れる |
| 4 | 1型/小児1型のデバイス希望申し送り行が B に無い | 再生成で消える |
| 5 | HTHL の【ルール追加】3行 + 「自由記入欄の内容も含める」が B に無い | 書式が崩れうる |
| 6 | DM基本 B の `◎甲状腺3項目追加済` | **発火しない死んだ行**（後述） |

### 誤検知だった（直していない）

**SAS の「目標HbA1c」「基本採血なし」は経路B にもあります**（[buildKartePrompt.js](lib/buildKartePrompt.js) 内）。
同期チェッカーが三項演算子の中の文字列を `⟨expr⟩` に潰しただけでした。HANDOFF_2026-08-22 で🔴にしたのは誤りです。

### 残り（直さなくてよい）

注記の文言差（`【アレルギー歴】` vs `【アレルギー歴】（…）` 等）と、`STAFF_FLAGS` / `⟨expr⟩` の見せかけ。出力は同じです。

---

## 2. `◎甲状腺3項目追加済` の一本化

**運用**（院長確認済み）: LDL-C 140mg/dl 以上で甲状腺3項目を追加する。

これは**申し送りの □行 で既に実現できていました**（条件=HLあり、DM基本/小児1型/HTHL/SAS の4フォーム）。
一方 `◎甲状腺3項目追加済` は `thyroidAdded` フラグを条件にしていましたが、
**このフラグを true にする入力UIがどのフォームにも存在せず**、一度も発火していませんでした。

運用上スタッフの判断が入らない（HL＝必ず追加）ため、チェック欄を新設せず **◎行を削除して□行に一本化**。

- HTHL / GDM / RH から ◎行（と RH の `□甲状腺3項目追加採血済`）を削除
- **GDM に □行を追加**（HL欄を持つのに □行が無く、他4フォームと不揃いだった）
- 各 `initialData` から dead field `thyroidAdded` を削除
- 結果、**DM基本 / 小児1型 / HTHL / SAS / GDM の5フォームで統一**

---

## 3. 採血後の GAD・CPR 算定根拠（新機能）

### 背景

GAD抗体・CPR は**糖尿病と診断されて初めて保険請求できる**。
診断がグレー（HbA1c 6.3〜6.6前後）の患者で追加してしまい、**査定された事例**があった。

運用は `問診（スタッフ）→ 採血（GAD・CPRを含めて実施）→ HbA1c確認 → グレーなら医師に確認 → 診断が付かなければ GAD・CPR を削除`。
医師の診察は全結果が揃う3日後以降なので、**判断する時点で医師は患者を診ていない**。

### 実装

**詳細画面**（問診一覧 → 患者を選択）に「🩸 採血後：糖尿病診断の確認結果」カードを追加（**DM基本のみ**）。

- 今回のHbA1c（必須）＋ 過去の空腹時血糖 / 食後血糖 / HbA1c（任意）
- 3択 → 申し送りの末尾（【診察にあたっての要望】の直前）に1行挿入
- **AI再生成は走らない**（[lib/dmDxNote.js](lib/dmDxNote.js) が文字列挿入するだけ）→「💾 編集を保存」で確定
- 選び直しても重複せず置換。「追記を取り消す」で削除

```
□今回HbA1c 6.4%と過去の空腹時血糖 132mg/dl・HbA1c 6.6%をあわせ、医師に確認し糖尿病の診断とした → GAD・CPRを追加
□今回HbA1c 6.4%（過去の空腹時血糖 118mg/dl）→ 医師に確認し糖尿病の診断は見送りとした → GAD・CPRを削除
□今回HbA1c 6.2%（診断基準未満）だが過去に糖尿病の診断歴があるため、GAD・CPRを追加
```

**なぜ過去の値まで書くのか**: 診断基準は 空腹時血糖126 / 食後血糖200 / HbA1c6.5% のうち**2項目**
（別項目2つ、または同一項目2回）。今回のHbA1c だけでは根拠にならないので、
「どの値と組み合わせて判断したか」が残らないと後から保険審査で説明できない。

**既存の `□血糖、HbA1cの結果により上段の診断を確定してください` とは別物**
（あちらは主病名 ＃糖尿病 / IGT / 正常耐糖能 を医師が選ぶための行）。両方並んで出てよい。

---

## 4. DM差分問診を4フォームに展開

SAS だけにあった「採血で糖尿病が判明したときの差分問診」を、
同じく**全例当院で事前採血を行う** 高血圧・脂質異常症 / 反応性低血糖 / 内分泌 でも使えるようにしました。

| フォーム | 統合後の主病名 |
|---|---|
| SAS | `＃SAS` → `＃糖尿病` |
| 高血圧・脂質異常症 | `＃糖尿病` → `＃HT` → `＃HL` |
| 反応性低血糖 | `＃糖尿病` → `♯反応性低血糖疑い` |
| 内分泌 | `＃糖尿病` のみ（下記） |

**フォーム固有の判断を2つ入れました。**

1. **HTHL**: IGT にチェックが入っていても、採血で糖尿病が確定した以上 `＃IGT` は出さず `＃糖尿病` に置き換える
2. **内分泌**: 「＃で始まる主病名行は絶対に推測して出力しない」ルールを持つフォームなので、
   `＃糖尿病` を**このルールの唯一の例外**として明示（採血で確定＝推測ではない）。
   内分泌の主病名は引き続き出力せず、`□主病名：医師の診察時に確定・記載` もそのまま残る

共通ロジックは [lib/dmDiff.js](lib/dmDiff.js) に切り出し。
`components/SASDmDiffEditor.js` → `components/DmDiffEditor.js` にリネームしました。

**この機能だけは再生成が必須**です（追記量が多くAIに書き直させる必要があるため）。

---

## 5. テスト

**60 → 88件**。

- `lib/dmDxNote.js` のユニット9件
- DM差分問診の振る舞い11件（dmDiff あり/なし × 4フォーム、＃IGT置換、＃順序、内分泌の例外扱い、療養計画書）
- **統合カルテのプロンプトのスナップショット4件**（今まで一度もテストされていなかった）
- `stabilize`（日付伏せ処理）のユニット3件

**リファクタの安全確認に効きました**: SAS の差分ロジックを `lib/dmDiff.js` に切り出したとき、
既存スナップショットが**1件も変わらないこと**で「出力は完全に同一」と即断できました。

---

## 🔴 実機確認（未実施・次セッションでお願いします）

```bash
npm run dev
```

1. `/gdm` → HLにチェック + 妊娠前体重を入力 → カルテに **`妊娠前:○kg`** と
   **`□健診・前医採血でLDL-C140mg/dl以上のため、甲状腺3項目を追加しました。`** が出るか
2. `/dm` → 糖尿病の症状をチェック → 保存 → 詳細画面で **🔄再生成** → 【糖尿病の症状】が消えずに残るか
3. `/dm` → 詳細画面 → 「🩸 採血後：糖尿病診断の確認結果」→ 追記 → 保存 → 申し送りの位置が正しいか
4. `/hthl` または `/rh` または `/endocrine` → 詳細画面 → DM差分問診を入力 → **🔄再生成** →
   `＃糖尿病` が付いた統合カルテになるか

---

## Obsidian（claude-code-memory vault）への統合 — ✅ 完了

ユーザー要望で、問診アプリを勤怠アプリと同じ vault に別アプリとして登録しました。

**vault 側は完了・push 済み**（`5039c9b`）。

- `Projects/Monshin/screens/*` を現状に更新。**2026-06-13 以来 2.5ヶ月 drift していた**
  （13→14フォーム、SAS専用→4フォーム、バグ防止機構の記載なし）
- `Projects/Monshin/project_prompt_sync_debt.md` 新設 —
  allow-list が 137→190行に膨らむ構造と「**検出はするが放置を許す**」限界を記録
- ローカルメモリ4件を vault へ移設。**push ルールの矛盾を1件解消**
  （worktree ブランチへの push は vault 側ルールに統一）
- `MEMORY.md` に問診の節を新設 / `reference_memory_workflow.md` に HANDOFF の置き場所を明文化

**junction も作成済み**（別セッションが同じ vault で並行稼働していたため一旦保留し、終了報告後に実施）。

```powershell
New-Item -ItemType Junction `
  -Path   "C:\Users\smats\.claude\projects\C--Users-smats-Documents-projects-dm-clinic-karte\memory" `
  -Target "C:\Users\smats\.claude\projects\C--Users-smats-Documents-projects-karte-voice-pad\memory"
```

**今後の運用**: 問診のコードを触ったセッションから、vault の `Projects/Monshin/screens/*` を
**同じセッション内で直接更新できます**。勤怠・カルテと同じ維持モードに入りました。
放置すると再び drift するので、機能を変えた回は必ず doc も直してください。

### 副産物: vault の実体パスの記述が間違っていました

運用 doc には「vault の実体は `karteplus-monorepo/memory`」と書かれていましたが、
**そこ自体が `karte-voice-pad/memory` への junction** でした。そのまま従うと二段 junction になります。

実体は **`C:\Users\smats\.claude\projects\C--Users-smats-Documents-projects-karte-voice-pad\memory`** です。
vault 側の `reference_memory_workflow.md` を訂正し、実体ありディレクトリからの移行手順も追記しました。

### HANDOFF の置き場所（2026-08-28 決定）

**分けません。本体を片方だけにします。**

| 何 | どこ |
|---|---|
| 問診の引き継ぎ**本体** | `dm-clinic-karte/HANDOFF_YYYY-MM-DD.md`（このファイル）— コードと同じ git で動く |
| vault の日次 HANDOFF | `HANDOFF_TO_NEXT_YYYY_MM_DD.md` — 1〜2行 + 本体へのリンクだけ |

vault の HANDOFF は**日付単位**であってアプリ単位ではないため（同じ日に複数アプリを触ると2ファイルに割れて
どちらも半分しか語らなくなる）。実際、今日の vault HANDOFF には別セッションの kinkan 分も同居しています。

---

## 未着手・積み残し

- **SessionStart / Stop フック**（junction は完了済み。勤怠と同じく 開始時の自動 pull + 未 push 警告）
- **allow-list に「理由」欄を必須化**する仕組み。今は理由なしで無限に積めるので、
  本物のバグが紛れても気付けない（137→190行に膨張中）
- 甲状腺6フォームは同期チェッカー**未対応**（1コンポーネント + `formType` prop 分岐で 1:1 にならない）
- ゴールデンテストは**経路B しかカバーしていない**（今回の GDM 修正は経路A なのでテストで検知できなかった）
- レート制限（`/api/generate*`）/ 操作ログ（audit_logs 相当）— セキュリティ監査から未着手のまま
- 本番カナリア（デプロイ後の疎通確認）
