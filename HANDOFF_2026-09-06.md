# HANDOFF 2026-09-06 — プロンプト二重管理の完全解消 + AI フリー撤回 + 統合ハイブリッド

前回: [HANDOFF_2026-09-03.md](HANDOFF_2026-09-03.md)（実運用バグ2件の修理・/help /handbook の統一）

## 最終 HEAD

```
0ba1e9e feat(prompt): 【仕事】に「就労なし」を明記（work === 'していない'）
```

別PCで作業を始める前に必ず:

```bash
git pull
```

このセッションの成果は全て `main` に push 済み。`npm run check`（スタイル114件・ゴールデン**151件**）と
`next build` はグリーン。**生成・保存の経路は変えていないので、外来の出力は今日時点で変わらない。**

---

## このセッションの結論を先に

| | |
|---|---|
| プロンプトの実装箇所 | **10 → 1**（`lib/buildKartePrompt.js`） |
| 削除 | プロンプト 737 行 + 死んだヘルパー約 350 行 + 同期チェッカー 260 行 + allow-list 65 行 |
| テスト | 88 → **151 件** |
| 新設 | `lib/karteFields.js` / `lib/buildKarteTemplate.js` / `lib/handoffNotes.js` / `scripts/golden/coverage.test.mjs` |
| 削除 | `scripts/check-prompt-sync.mjs` / `scripts/prompt-sync-allow.json`（役目を終えた） |

---

## 1. プロンプトを `buildKartePrompt()` に一本化（commit `320118c` `47a692c`）

### やったこと

コンポーネント 9 ファイル（8 フォーム + 甲状腺）のプロンプト組立を**全廃**し、初回生成も再生成も
`lib/buildKartePrompt.js` を呼ぶ形にした。**14 フォーム全て**。

```js
// components/*IntakeTool.js の generateKarte() 内
const { prompt, max_tokens } = buildKartePrompt("DM基本", data);   // 甲状腺は meta.dbLabel
```

CLAUDE.md で 4 ヶ月「最重要注意点」だったプロンプト二重管理が、**構造的に消滅した**。

### ★ 切り替える前に「出力が変わらない」ことを証明した

コンポーネントの **JSX 直前までを切り出して実行**し、`fetch` をスタブして
**実際に `/api/generate` へ送られるプロンプト**を捕まえ、`buildKartePrompt()` の出力とバイト比較した。

```
8 フォーム × 6 条件（未入力 / フィクスチャ / スタッフフラグ全部 / 医師希望なし / 音声入力+要DR確認 / dmDiff）
+ 甲状腺 6 × 3 条件 = 66 ケース 一致
```

★**コードを写して比べるのでなく、本物を実行して捕まえる**ので写し間違いが原理的に起きない。
JSX は `return (` 以降にしか無いので、**React も JSX 変換も要らない**。
一本化後は同じコードが両経路を担うため、この検証ハーネスは repo に残していない（scratchpad で実行した）。

### 🔴 この証明で見つかった不整合 5 件（同期チェッカーが 3ヶ月半 一度も報告しなかった）

1. ★**音声入力ブロックの文言が経路ごとに違った** — `${...}` の中なのでチェッカーは `⟨expr⟩` に潰していた。
   **音声入力は毎日使う機能**なので、実質ほぼ全例で初回生成と再生成に違う指示が飛んでいた
2. 経路B の `${hasDm ? … : ''}` が単独行にあり、dmDiff が無いとき**空行を残していた**（既知バグ④の再発）
3. 甲状腺の出力フォーマットが経路A ではプレースホルダ（`【喫煙歴】（整形済みテキスト）`）、経路B では値を埋めていた
4. プロンプト末尾の改行の有無
5. SAS の `max_tokens` が経路A=1500 / 経路B=1800（→ 1800 に統一）

### 同期チェッカーの後継

`scripts/check-prompt-sync.mjs`（260行）と `prompt-sync-allow.json`（65行）は**削除**した。
比較する相手が無くなったため。代わりに `prompt.test.mjs` に 2 本:

- `components/*IntakeTool.js` に `` const prompt = ` `` が現れたら落ちる
- 全 IntakeTool が `buildKartePrompt()` を呼んでいなければ落ちる

> ★★ **教訓**: **「差分を検出する仕組み」より「差分が存在し得ない構造」の方が強い。**
> チェッカーは CI を緑にし続けながら、毎日使う音声入力の不整合を一度も報告しなかった。
> `${...}` を潰して比較する以上、**変数の中身の違いは原理的に見えない**。
> **検出器を作ったら、それが見ていない範囲を必ず言語化すること。**

## 2. 申し送りの決定論化（`lib/handoffNotes.js`）

「（新患2枠取得済の場合）□…」のような**条件文を AI に判定させていた行**のうち、判定材料が
form_data に揃っているものを純粋関数に移した。プロンプトには**該当する □ 行そのもの**が入る。

この過程で **医師希望の表記が 3 種類あった**のが判明した（経路A 8フォーム = `□医師希望：女性医師` /
経路B 全フォーム・甲状腺 = `□女性医師希望`）。CLAUDE.md の規定どおり前者に統一。

## 3. 「AI フリー」を撤回 → 統合だけ AI に頼む（`lib/buildKarteTemplate.js`）

### 経緯

「問診データはほぼ全て構造化されており、音声は入力時点で `voiceSummary.js` が AI 整形済み。
だからカルテ生成は AI 呼び出しゼロで組める」と考えて実装した（commit `829e775`）。

**これは誤りだった。** 今のプロンプトは音声テキストを構造化データと **統合せよ**と指示している:

```
【音声入力からのAI整形済み現病歴(必ず受診理由サマリーに統合)】
【音声入力からのAI整形済み既往歴(♯既往疾患セクションに統合、内容に応じて他院管理表記等も補完)】
```

JS で単純に置き換えると、実測で次が起きた:

- 受診理由: 音声があると**紹介元・科・紹介理由・自由記入が丸ごと落ちる**
- ♯既往: 同じ疾患が構造化と音声の両方にあると**2 行に重複**

★**AI が入る場所は 2 箇所ある**: ①**入力時**（音声 → 文・`voiceSummary.js`）②**出力時**（統合）。
テンプレート化が消せるのは②だけで①は残る。

### 案 2（統合ハイブリッド）の形 — commit `30633bd`

```js
buildKarteTemplate(form_type, d)             // AI なし版（フォールバック）
buildKarteTemplate(form_type, d, { merged }) // AI の統合結果を差し込んだ版
buildMergePrompt(form_type, d)               // 統合だけを頼む小さいプロンプト
parseMergeResponse(text)                     // 壊れていたら null → 素組みに自動フォールバック
```

- 詳細画面の「テンプレート版」は**開いたら一度だけ自動で統合**する（commit `390f77c`）。
  統合しない版は落ちると分かっているので**それを見せて比較させない**
- **生成経路を差し替えたら統合は生成の一部になるので、ボタンという概念自体が無くなる**

### コスト（実測・DM基本・音声あり）

| | 入力 | 出力 |
|---|---|---|
| 現状（全文生成） | 6,363 字 | 906 字 |
| 案 2（統合のみ） | **651 字** | 約 200 字 |

**入力 90%減 / 出力 78%減 / 生成部分のコスト 85%減。**
⚠️ ただし**総額では 32% 減**にとどまる（音声整形 2 回が全体の 63% を占めるため）。

## 4. 表記の確定（院長判断）— 原則は「未入力」と「なし」を書き分ける

| 項目 | 確定 |
|---|---|
| 【眼科通院歴】未入力 | **空欄**（「未受診」と書かない） |
| 　　同 受けていない / 今後受ける予定 | `未受診` / `今後受ける予定` |
| 【健診】未選択 | **空欄**（行は残す） |
| 【仕事】 | `していない` → **`就労なし・<活動量>`** / 未入力 → 空欄 |
| アレルギー警告 | 薬剤アレルギーが無ければ**行ごと出さない**（花粉・フルーツ・金属等は除外） |
| 【FH】に HL | 足さない（DM基本のフォームは持たない。HTHL/RH/内分泌 は持つ） |
| ♯重要既往 | `♯胃癌（胃切除後：…）` `♯膵臓癌（術後：…）` `♯IHD：PCI後（…）` |

★**今の AI 版はこの区別をしていない**（未入力でも「未受診」「なし」と書きうる）= テンプレート化で**質が上がる**箇所。

### 実患者 1 例での比較で見つかったこと

| | AI 版 | テンプレート版 |
|---|---|---|
| ＃糖尿病 の後 | **空行あり** | 空行なし ← ✅ 正しい（♯他院管理が無いので不要） |
| 【仕事】 | 仕事なし**・立っていることが多い** | 就労なし ← 🔴 **活動量を落としていた**（`42a44fa` で修正） |

★ **この落ちは全フィールド走査では検出できない**（activity は「している」経路では使われているため）。
⇒ **検証は 2 段構え**: 機械の走査（CI）＋ **実データでの突き合わせ（人）**。

## 5. 取りこぼし検査（`scripts/golden/coverage.test.mjs`）

form_data の全フィールドを 1 つずつ書き換えて、**カルテ本文か統合プロンプトのどちらかが変わるか**を見る。
どちらも変わらなければ、その項目はカルテに一切影響していない = **情報が黙って落ちている**。

- 例外は `ALLOWED_UNREFERENCED` に**理由付きで**登録する（理由が書けないものは直す）
- 逆に「理由リストに載っているのに実は使われている」も検出する（棚卸し）
- 🔴 **他フォームへ展開するときは毎フォームこれを足すこと**

★ これがあるので「質を落とさない」が**約束でなく検査**になる。
今の AI 版は JSON 全体が「見えている」が**使うかは毎回変わる**（2026-04 の【糖尿病の症状】欠落はそこで起きた）。

### この検査で見つかったフィクスチャの不備（修正済）

- DM基本は生活情報を `lifestyle`、医療情報を `history` に分けて持つのに、**両方に同じ内容**を入れていた
- DM基本の家族歴に `hl` は無いのに入っていた
- `disease.insulin` が実データの `insulinUse` と違っていた
- `history.eyeVisiting` は**フォームの UI にも無い遺物**（initialData に残っているだけ）

⇒ ★**テストデータが実データと違うと、テストは通るのに本番で落ちる**。
新フォームを足すときは**フィクスチャを `initialData` から起こすこと**。

## 6. 🔴 次にやること

1. ★**音声入力を使った初診を 1 例、詳細画面で見比べる**
   （`/detail/[id]` → 「テンプレート版」を展開 → 自動で統合が走る）
   今日見たのは**音声なしの症例**で、統合の仕事がほぼ無いケースだった。当院の実運用はほぼ全例が音声あり。
   見るのは 2 点: ①音声の現病歴と紹介元・自由記入が**混ざるか** ②♯既往が**重複せず 1 行にまとまるか**
2. 問題なければ **DM基本の生成経路を統合版に切り替える**（作業 30 分）。切替後は**数日カルテを目視**
3. 他 13 フォームへテンプレート展開（**毎フォーム coverage テストを足す**）
4. 甲状腺 6 フォームは一本化済だが**テンプレート版は未着手**

## 7. 残課題（前回から継続）

- レート制限（`/api/generate*`）
- 操作ログ（audit_logs 相当）
- prompt caching の実装 — ★**損益分岐は月 392 件（全施設合計）**と実測できた。
  当院単独では書込代の方が高くつくので**入れない**。商品化して束ねるときに効く
- T1D / PedT1D / HTHL / GDM / RH の step 大規模再編（B案）

## 8. 参考

- 戦略・商品化の議論は memory 側（`HANDOFF_TO_NEXT_2026_09_06_2.md`）に記録。
  要点だけ書くと **問診を先兵に反転**・**AI 原価は 1 施設あたり月 ¥32 で決着**・
  **プロンプトキャッシュの共有には「1 キーで束ねる」契約形態が前提**
